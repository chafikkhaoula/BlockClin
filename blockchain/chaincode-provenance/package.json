{
  "name": "blockclin-provenance",
  "version": "1.0.0",
  "description": "Hyperledger Fabric chaincode for BlockClin clinical-data provenance",
  "main": "index.js",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "fabric-chaincode-node start",
    "test": "node --check index.js && node --check lib/provenance-contract.js"
  },
  "license": "Apache-2.0",
  "dependencies": {
    "fabric-contract-api": "~2.5",
    "fabric-shim": "~2.5",
    "json-stringify-deterministic": "^1.0.0",
    "sort-keys-recursive": "^2.1.0"
  }
}
